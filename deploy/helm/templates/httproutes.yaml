# Copyright (c) 2025, NVIDIA CORPORATION.  All rights reserved.
#
# =============================================================================
# HTTPRoutes for NVIDIA Config Manager Services
# =============================================================================
# This template creates HTTPRoutes for all enabled services in the platform
# Uses subdomain-based routing (each service gets its own hostname)
#
# Auth Bypass Routes:
# For services that need to support both OIDC/JWT and native token auth,
# separate HTTPRoutes bypass the browser proxy so the service can validate a
# Bearer JWT or its native token directly.
# =============================================================================

# =============================================================================
# Service (svc-*) HTTPRoute Helper Template
# =============================================================================
{{- define "nv-config-manager.stripIdentityHeaders" }}
filters:
- type: RequestHeaderModifier
  requestHeaderModifier:
    remove:
    - ssl-client-cert
    - X-Auth-Request-Email
    - X-Auth-Request-User
    - X-Auth-Request-Groups
    - X-Auth-Request-Preferred-Username
    - X-Auth-Request-Name
    - X-Auth-Request-Roles
    - X-Auth-Request-Subject
    - X-Auth-Request-Access-Token
    - X-Forwarded-Email
    - X-Forwarded-User
    - X-Forwarded-Groups
    - X-Forwarded-Preferred-Username
    - X-Forwarded-Access-Token
{{- end }}

{{/* Labels HTTPRoutes for controller-specific timeout policies. */}}
{{- define "nv-config-manager.httpRoutePolicyLabels" -}}
labels:
  {{- include "nv-config-manager.labels" .root | nindent 2 }}
  nv-config-manager.nvidia.com/timeout-profile: {{ .timeoutProfile | default "default" | quote }}
{{- end }}

# Creates an HTTPRoute for the svc-* hostname (JWT-only, no OIDC).
# These routes allow CLI/machine clients to authenticate with just a JWT
# bearer token without being redirected to the OIDC login flow.
# These routes bypass the browser proxy and let application middleware validate
# Bearer JWTs without triggering a browser redirect.
# =============================================================================
{{- define "nv-config-manager.svcHTTPRoute" }}
{{- if and $.root.Values.oidc.enabled $.svcHostname }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $.routeName }}-svc
  namespace: {{ $.root.Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" $.root "timeoutProfile" ($.timeoutProfile | default "default")) | nindent 2 }}
  annotations:
    # JWT-only service route - OIDC disabled, CLI/machine access
    nv-config-manager.nvidia.com/svc-route: "true"
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" $.root | nindent 2 }}
  hostnames:
  - {{ $.svcHostname | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" $.root | nindent 4 }}
    backendRefs:
    - name: {{ $.backendName }}
      port: {{ $.backendPort }}
{{- end }}
{{- end }}

{{- define "nv-config-manager.browserBackendRefs" }}
backendRefs:
{{- if $.root.Values.oidc.enabled }}
- name: {{ include "nv-config-manager.componentName" (dict "root" $.root "component" "oidc-proxy") }}
  port: {{ $.proxyPort }}
{{- else }}
- name: {{ $.backendName }}
  port: {{ $.backendPort }}
{{- end }}
{{- end }}

# =============================================================================
# Auth Bypass HTTPRoute Helper Template
# =============================================================================
# Creates a separate HTTPRoute for paths that bypass the browser proxy while
# retaining application-native Bearer JWT or service token authentication.
# =============================================================================
{{- define "nv-config-manager.authBypassHTTPRoute" }}
{{- if and $.root.Values.oidc.enabled $.root.Values.gateway.authBypass.enabled $.enabled (gt (len $.paths) 0) }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $.routeName }}-auth-bypass
  namespace: {{ $.root.Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" $.root "timeoutProfile" ($.timeoutProfile | default "default")) | nindent 2 }}
  annotations:
    # Auth bypass route - application-native authentication, no browser redirect
    nv-config-manager.nvidia.com/auth-bypass: "true"
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" $.root | nindent 2 }}
  hostnames:
  - {{ $.hostname | quote }}
  rules:
  {{- range $.paths }}
  - matches:
    - path:
        type: PathPrefix
        value: {{ . }}
      {{- if $.requireAuthorization }}
      headers:
      - name: Authorization
        type: RegularExpression
        value: "^(Bearer|Token) .+$"
      {{- end }}
    {{- include "nv-config-manager.stripIdentityHeaders" $.root | nindent 4 }}
    backendRefs:
    - name: {{ $.backendName }}
      port: {{ $.backendPort }}
  {{- end }}
{{- end }}
{{- end }}

{{- $renderName := include "nv-config-manager.componentName" (dict "root" . "component" "render") }}
{{- $ztpName := include "nv-config-manager.componentName" (dict "root" . "component" "ztp") }}
{{- $dhcpName := include "nv-config-manager.componentName" (dict "root" . "component" "dhcp") }}
{{- $temporalName := include "nv-config-manager.componentName" (dict "root" . "component" "temporal") }}
{{- $uiName := include "nv-config-manager.componentName" (dict "root" . "component" "ui") }}
{{- $nautobotName := include "nv-config-manager.componentName" (dict "root" . "component" "nautobot") }}
{{- $configStoreName := include "nv-config-manager.componentName" (dict "root" . "component" "config-store") }}
{{- $mcpName := include "nv-config-manager.componentName" (dict "root" . "component" "mcp") }}
{{- $oidcProxyName := include "nv-config-manager.componentName" (dict "root" . "component" "oidc-proxy") }}

{{- if .Values.gateway.enabled }}

# Shared OIDC callback route. A dedicated proxy serves this endpoint so OIDC
# remains functional even when the UI component is disabled.
{{- if .Values.oidc.enabled }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-callback" $oidcProxyName | trunc 63 | trimSuffix "-" }}
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ .Values.gateway.baseHostname | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /oauth2
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    backendRefs:
    - name: {{ $oidcProxyName }}
      port: 9109
{{- end }}

# Render Service svc-* HTTPRoute (JWT-only)
{{- if .Values.renderService.enabled }}
{{- include "nv-config-manager.svcHTTPRoute" (dict
    "root" .
    "routeName" $renderName
    "svcHostname" (tpl (.Values.renderService.gateway.svcHostname | default "") .)
    "backendName" (printf "%s-api" $renderName)
    "backendPort" 9000
) }}
{{- end }}

# Render Service HTTPRoute
{{- if .Values.renderService.enabled }}
{{- if .Values.renderService.gateway.authBypass }}
{{- include "nv-config-manager.authBypassHTTPRoute" (dict
    "root" .
    "routeName" $renderName
    "enabled" (.Values.renderService.gateway.authBypass.enabled | default false)
    "paths" (.Values.renderService.gateway.authBypass.paths | default list)
    "hostname" (tpl .Values.renderService.gateway.hostname .)
    "backendName" (printf "%s-api" $renderName)
    "backendPort" 9000
) }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $renderName }}
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ tpl .Values.renderService.gateway.hostname . | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    {{- include "nv-config-manager.browserBackendRefs" (dict "root" . "backendName" (printf "%s-api" $renderName) "backendPort" 9000 "proxyPort" 9101) | nindent 4 }}
{{- end }}

# Network ZTP svc-* HTTPRoute (JWT-only)
{{- if .Values.networkZtp.enabled }}
{{- include "nv-config-manager.svcHTTPRoute" (dict
    "root" .
    "routeName" $ztpName
    "svcHostname" (tpl (.Values.networkZtp.gateway.svcHostname | default "") .)
    "backendName" (printf "%s-api" $ztpName)
    "backendPort" 9000
    "timeoutProfile" "upload"
) }}
{{- end }}

# Network ZTP HTTPRoute (for user/SSO access via gateway)
{{- if .Values.networkZtp.enabled }}
{{- if .Values.networkZtp.gateway.authBypass }}
{{- include "nv-config-manager.authBypassHTTPRoute" (dict
    "root" .
    "routeName" $ztpName
    "enabled" (.Values.networkZtp.gateway.authBypass.enabled | default false)
    "paths" (.Values.networkZtp.gateway.authBypass.paths | default list)
    "hostname" (tpl .Values.networkZtp.gateway.hostname .)
    "backendName" (printf "%s-api" $ztpName)
    "backendPort" 9000
    "timeoutProfile" "upload"
) }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $ztpName }}
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" . "timeoutProfile" "upload") | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ tpl .Values.networkZtp.gateway.hostname . | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    {{- include "nv-config-manager.browserBackendRefs" (dict "root" . "backendName" (printf "%s-api" $ztpName) "backendPort" 9000 "proxyPort" 9102) | nindent 4 }}
{{- end }}

# Network DHCP svc-* HTTPRoute (JWT-only)
{{- if .Values.networkDhcp.enabled }}
{{- include "nv-config-manager.svcHTTPRoute" (dict
    "root" .
    "routeName" $dhcpName
    "svcHostname" (tpl (.Values.networkDhcp.gateway.svcHostname | default "") .)
    "backendName" (printf "%s-internal" $dhcpName)
    "backendPort" 9000
) }}
{{- end }}

# Network DHCP HTTPRoute (for user/SSO access via gateway)
{{- if .Values.networkDhcp.enabled }}
{{- if .Values.networkDhcp.gateway.authBypass }}
{{- include "nv-config-manager.authBypassHTTPRoute" (dict
    "root" .
    "routeName" $dhcpName
    "enabled" (.Values.networkDhcp.gateway.authBypass.enabled | default false)
    "paths" (.Values.networkDhcp.gateway.authBypass.paths | default list)
    "hostname" (tpl .Values.networkDhcp.gateway.hostname .)
    "backendName" (printf "%s-internal" $dhcpName)
    "backendPort" 9000
) }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $dhcpName }}
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ tpl .Values.networkDhcp.gateway.hostname . | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    {{- include "nv-config-manager.browserBackendRefs" (dict "root" . "backendName" (printf "%s-internal" $dhcpName) "backendPort" 9000 "proxyPort" 9103) | nindent 4 }}
{{- end }}

# Temporal API svc-* HTTPRoute (JWT-only)
{{- if .Values.temporal.enabled }}
{{- include "nv-config-manager.svcHTTPRoute" (dict
    "root" .
    "routeName" (printf "%s-api" $temporalName)
    "svcHostname" (tpl (.Values.temporal.gateway.api.svcHostname | default "") .)
    "backendName" (printf "%s-api" $temporalName)
    "backendPort" 9000
) }}
{{- end }}

# Temporal API HTTPRoute
{{- if .Values.temporal.enabled }}
{{- if .Values.temporal.gateway.api.authBypass }}
{{- include "nv-config-manager.authBypassHTTPRoute" (dict
    "root" .
    "routeName" (printf "%s-api" $temporalName)
    "enabled" (.Values.temporal.gateway.api.authBypass.enabled | default false)
    "paths" (.Values.temporal.gateway.api.authBypass.paths | default list)
    "hostname" (tpl .Values.temporal.gateway.api.hostname .)
    "backendName" (printf "%s-api" $temporalName)
    "backendPort" 9000
) }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $temporalName }}-api
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ tpl .Values.temporal.gateway.api.hostname . | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    {{- include "nv-config-manager.browserBackendRefs" (dict "root" . "backendName" (printf "%s-api" $temporalName) "backendPort" 9000 "proxyPort" 9104) | nindent 4 }}
---
# Temporal Web UI HTTPRoute (temporal.<base>) - OIDC-only, direct to app port
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $temporalName }}-web
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ tpl .Values.temporal.gateway.devUi.hostname . | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    {{- include "nv-config-manager.browserBackendRefs" (dict "root" . "backendName" (printf "%s-web-service" $temporalName) "backendPort" 8080 "proxyPort" 9105) | nindent 4 }}
{{- end }}

---
# Unified UI HTTPRoute (at base hostname) - OIDC-only, direct to app port
{{- if .Values.ui.enabled }}
{{- if and .Values.oidc.enabled .Values.oidc.authUtility.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $uiName }}-auth-utility
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
  annotations:
    # Allows unauthenticated access to public auth utility endpoints.
    nv-config-manager.nvidia.com/no-auth: "true"
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ .Values.gateway.baseHostname | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /auth/logout
    - path:
        type: PathPrefix
        value: /auth/discovery
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    backendRefs:
    - name: {{ $uiName }}
      port: 3000
---
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $uiName }}
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ .Values.gateway.baseHostname | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    {{- include "nv-config-manager.browserBackendRefs" (dict "root" . "backendName" $uiName "backendPort" 3000 "proxyPort" 9100) | nindent 4 }}
{{- end }}

# Nautobot svc-* HTTPRoute (JWT-only)
{{- if and .Values.nautobot.enabled .Values.externalServices.nautobot.local }}
{{- include "nv-config-manager.svcHTTPRoute" (dict
    "root" .
    "routeName" $nautobotName
    "svcHostname" (tpl (.Values.nautobot.gateway.svcHostname | default "") .)
    "backendName" $nautobotName
    "backendPort" 80
) }}
{{- end }}

# Nautobot HTTPRoute (when deployed locally)
{{- if and .Values.nautobot.enabled .Values.externalServices.nautobot.local }}
# -----------------------------------------------------------------------------
# Nautobot Auth Bypass HTTPRoute (for /api/)
# -----------------------------------------------------------------------------
# Nautobot supports multiple auth methods:
#   - OIDC/JWT (browser users via SSO)
#   - Native API tokens (Authorization: Token <token>)
#   - Remote user auth (trusts X-Auth-Request-User header from gateway)
#
# This route bypasses the browser proxy only when an Authorization header is
# present, allowing native API tokens without exposing browser API calls. SSO
# browser requests have no client Authorization header and stay on the main
# route so the proxy can supply trusted remote-user headers.
# -----------------------------------------------------------------------------
{{- include "nv-config-manager.authBypassHTTPRoute" (dict
    "root" .
    "routeName" $nautobotName
    "enabled" .Values.nautobot.gateway.authBypass.enabled
    "paths" .Values.nautobot.gateway.authBypass.paths
    "hostname" (tpl .Values.nautobot.gateway.hostname .)
    "backendName" $nautobotName
    "backendPort" 80
    "requireAuthorization" true
) }}
# Main Nautobot HTTPRoute (for UI/browser access with OIDC)
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $nautobotName }}
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ tpl .Values.nautobot.gateway.hostname . | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    {{- include "nv-config-manager.browserBackendRefs" (dict "root" . "backendName" $nautobotName "backendPort" 80 "proxyPort" 9106) | nindent 4 }}
{{- end }}

# Config Store API svc-* HTTPRoute (JWT-only)
{{- if .Values.configStore.enabled }}
{{- include "nv-config-manager.svcHTTPRoute" (dict
    "root" .
    "routeName" (printf "%s-api" $configStoreName)
    "svcHostname" (tpl (.Values.configStore.gateway.api.svcHostname | default "") .)
    "backendName" (printf "%s-api" $configStoreName)
    "backendPort" 9000
) }}
{{- end }}

# Config Store API HTTPRoute
{{- if .Values.configStore.enabled }}
{{- if .Values.configStore.gateway.api.authBypass }}
{{- include "nv-config-manager.authBypassHTTPRoute" (dict
    "root" .
    "routeName" (printf "%s-api" $configStoreName)
    "enabled" (.Values.configStore.gateway.api.authBypass.enabled | default false)
    "paths" (.Values.configStore.gateway.api.authBypass.paths | default list)
    "hostname" (tpl .Values.configStore.gateway.api.hostname .)
    "backendName" (printf "%s-api" $configStoreName)
    "backendPort" 9000
) }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $configStoreName }}-api
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ tpl .Values.configStore.gateway.api.hostname . | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    {{- include "nv-config-manager.browserBackendRefs" (dict "root" . "backendName" (printf "%s-api" $configStoreName) "backendPort" 9000 "proxyPort" 9107) | nindent 4 }}
{{- end }}

# MCP svc-* HTTPRoute (JWT-only)
{{- if .Values.mcp.enabled }}
{{- include "nv-config-manager.svcHTTPRoute" (dict
    "root" .
    "routeName" $mcpName
    "svcHostname" (tpl (.Values.mcp.gateway.svcHostname | default "") .)
    "backendName" $mcpName
    "backendPort" 9000
) }}
{{- end }}

# MCP HTTPRoute
{{- if .Values.mcp.enabled }}
{{- if .Values.mcp.gateway.authBypass }}
{{- include "nv-config-manager.authBypassHTTPRoute" (dict
    "root" .
    "routeName" $mcpName
    "enabled" (.Values.mcp.gateway.authBypass.enabled | default false)
    "paths" (.Values.mcp.gateway.authBypass.paths | default list)
    "hostname" (tpl .Values.mcp.gateway.hostname .)
    "backendName" $mcpName
    "backendPort" 9000
) }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $mcpName }}
  namespace: {{ .Values.global.namespace }}
  {{- include "nv-config-manager.httpRoutePolicyLabels" (dict "root" .) | nindent 2 }}
spec:
  parentRefs:
  {{- include "nv-config-manager.gatewayParentRef" . | nindent 2 }}
  hostnames:
  - {{ tpl .Values.mcp.gateway.hostname . | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    {{- include "nv-config-manager.stripIdentityHeaders" . | nindent 4 }}
    {{- include "nv-config-manager.browserBackendRefs" (dict "root" . "backendName" $mcpName "backendPort" 9000 "proxyPort" 9108) | nindent 4 }}
{{- end }}

{{- end }}
