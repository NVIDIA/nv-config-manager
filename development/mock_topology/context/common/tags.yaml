---
# Tags are managed by the bootstrap job (nv_config_manager_jobs/data/tags.yaml)
# The json.tags variable is dynamically built from device data in context/__init__.py
# and filters out bootstrap-managed tags to avoid conflicts.
#
# This file previously contained common_tags which was not used by any template.
