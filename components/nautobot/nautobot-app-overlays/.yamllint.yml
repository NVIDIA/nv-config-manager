---
extends: "default"

ignore: |
  .venv/

rules:
  document-start: "disable"
  indentation:
    spaces: 2
    indent-sequences: true
  line-length:
    max: 120
  truthy:
    allowed-values: ["true", "false", "yes", "no"]
